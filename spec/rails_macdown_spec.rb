require 'spec_helper'

describe RailsMacdown do
  it 'has a version number' do
    expect(RailsMacdown::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
